#include <iostream>
#include <algorithm>
#include <stack>
using namespace std;
const int N = 1e5;
int n, m, a[N];
int main() {
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
}
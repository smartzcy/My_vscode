#include <iostream>
using namespace std;
int n, m;
int main() {
    int T;
    cin >> T;
    for (int k = 1; k <= T; k++) {
        cout << "Case #" << k << ":" << endl;
        cin >> n >> m;
        for (int i = 1; i <= n; i++) {
        }
        while (m--) {
            int k, x, y;
            cin >> k >> x >> y;
            if (k == 0) {
            }
        }
    }
}
#include <iostream>
#include <algorithm>
using namespace std;
const int N = 1e5;
int n, m;
int main() {
    int num;
    int k = 0;
    while (cin >> num) {
        k++;
        if (num == 250) {
            cout << k << endl;
            return 0;
        }
    }
}
#include <iostream>
#include <algorithm>
using namespace std;
const int N = 1e5;
int T, M;
pair<int, int> vec[N];
int main() {
    cin >> T >> M;
    for (int i = 0; i <= M; i++) {
        int u, v;
        cin >> u >> v;
        vec[i] = {u, v};
        
    }
}